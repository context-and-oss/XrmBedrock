{
  "name": "webresources",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HTML web resources and form scripts for XrmBedrock",
  "scripts": {
    "dev": "node dev.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "typings": "cd ../../.. && dotnet tool restore && dotnet xdt",
    "check": "npm run typecheck && npm run build && cd ../../.. && dotnet xrmsync config validate",
    "deploy": "npm run build && cd ../../.. && dotnet tool restore && dotnet xrmsync --profile webresources"
  },
  "dependencies": {
    "@delegateas/xrmquery": "7.0.0-preview.3"
  },
  "devDependencies": {
    "@types/xrm": "^9.0.96",
    "esbuild": "^0.28.0",
    "typescript": "^5.9.0"
  }
}
